name: Visual Format Closeout

on:
  push:
    branches: [prd-02-playable-core]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: prd-02-playable-core
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npx prettier --write src/presentation/DevScene.ts
      - name: Commit format-only closeout if needed
        shell: bash
        run: |
          if [ -n "$(git status --porcelain -- src/presentation/DevScene.ts)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/presentation/DevScene.ts
            git commit -m "style: format visual combat pass"
            git push origin HEAD:prd-02-playable-core
          fi
